# Excel To JSON 插件 — 操作流程文档

> 适用版本：Unity Editor 插件 v1.0
> 目标用户：策划、程序
> 难度：零基础可操作

---

## 一、准备工作（只需做一次）

### 1.1 确认 DLL 文件齐全

在 Unity 项目里打开以下目录，确认有 **6 个 DLL 文件**：

```
Assets/Plugins/ExcelToJsonPlugin/Dependencies/
```

| 文件名 | 作用 |
|--------|------|
| `NPOI.dll` | Excel 读取核心库 |
| `NPOI.OOXML.dll` | .xlsx 格式支持 |
| `NPOI.OpenXml4Net.dll` | ZIP 压缩支持 |
| `NPOI.OpenXmlFormats.dll` | 格式转换器 |
| `BouncyCastle.Crypto.dll` | 加密支持 |
| `ICSharpCode.SharpZipLib.dll` | ZIP 解压写入 |

如果少了 `ICSharpCode.SharpZipLib.dll`，下载脚本在项目根目录 `download_sharpziplib.ps1`，双击运行即可。

### 1.2 确认 Unity 设置

1. 打开 Unity 编辑器
2. 顶部菜单 → **Edit → Project Settings → Player**
3. 找到 **Api Compatibility Level**
4. 设置为 `.NET Framework`（不是 .NET Standard）
5. 如果修改了，Unity 会要求重启，点确定

---

## 二、打开可视化编辑器

### 2.1 打开主面板

**Unity 顶部菜单 → Window → Excel Data Manager**

会弹出一个窗口，左侧是文件列表，右侧是功能标签页。

> 如果菜单里找不到，说明代码有编译错误。先在 Unity 里点 **Window → General → Console** 看有没有红色错误，有就告诉我。

### 2.2 第一次使用：生成示例数据

**Unity 顶部菜单 → Window → Excel Data Manager → 生成示例数据**

点击后会弹出对话框提示"示例数据已生成"。关闭对话框后，左侧文件列表会自动刷新，出现 `Item.xlsx` 和 `SkillEnum.xlsx` 两个文件。

---

## 三、查看数据预览

### 3.1 展开文件

在左侧文件树中，点击 `Item.xlsx` 旁边的展开箭头（或直接点文件名），会看到：

```
📄 Item.xlsx
  📋 Weapon    ← 点击这个
  📋 Armor
```

### 3.2 预览数据

点击 `Weapon`，右侧切换到 **数据预览** 标签页，你会看到：

```
id │ name   │ attack │ defense │ price │ rare │ element │ desc
int│ string │ int    │ int     │ int   │ enum │ string  │ string
───┼────────┼────────┼─────────┼───────┼──────┼─────────┼──────
1001│ 新手剑 │ 12    │ 0       │ 100  │ 1    │ 火      │ 一把普通的铁剑
1002│ 铁剑   │ 25    │ 5       │ 300  │ 2    │ 无      │ 经过锻造的坚固铁剑
...
```

- **蓝色**列名 = 数值类型（int、float）
- **绿色**列名 = 文本类型（string）
- **紫色**列名 = 引用类型（ref、enum）

> 数据太多时，用底部的 ◀ ▶ 按钮翻页，每页 100 行。搜索框输入 `新手剑` 可以快速定位。

---

## 四、导出数据

### 4.1 校验数据

先看看数据有没有问题：

1. 点击工具栏的 **校验** 按钮
2. 等待进度条跑完
3. 自动切换到 **校验** 标签页

如果显示"校验通过"，说明数据完全没问题。如果有错误，会列出具体哪个表的哪一行哪一列有问题。

### 4.2 导出

1. 点击工具栏的 **全部导出** 按钮
2. 等待进度条跑完
3. 自动切换到 **校验** 标签页展示结果

导出成功后，数据会生成到 `Assets/Data/` 目录：

```
Assets/Data/
├── Weapon.asset       ← Unity 数据资产
├── Weapon.json        ← JSON 文件（用于热更新）
├── Armor.asset
├── Armor.json
├── SkillEnum.asset
└── SkillEnum.json
```

同时自动生成 C# 代码到 `Assets/Scripts/Generated/`：

```
Assets/Scripts/Generated/
├── Data/
│   ├── WeaponRow.cs       ← 数据行类
│   ├── ArmorRow.cs
│   └── SkillEnumRow.cs
└── Tables/
    ├── WeaponTable.cs     ← 容器类
    ├── ArmorTable.cs
    └── SkillEnumTable.cs
```

---

## 五、在游戏里使用数据（给程序看的）

### 5.1 创建数据管理器

1. 打开 **Editor Window → Runtime API** 标签页
2. 点击 **自动生成 DataManager GameObject** 按钮
3. 场景里会多出一个 `DataManager` 对象，Inspector 里已经自动填好了所有数据表

### 5.2 写代码读取数据

```csharp
// 获取表引用
var weaponTable = DataManager.Instance.GetTable<WeaponTable>();

// 按 ID 查询（O(1)，非常快）
var weapon = weaponTable.Get(1001);
Debug.Log(weapon.name);   // 输出: 新手剑
Debug.Log(weapon.attack); // 输出: 12

// 获取全部数据
foreach (var w in weaponTable.GetAll())
{
    Debug.Log($"{w.id}: {w.name}");
}

// 条件筛选
var highAttack = weaponTable.Find(w => w.attack > 30);
```

### 5.3 按外键关联查询

Weapon 表的 `rare` 字段类型是 `enum:SkillEnum`，它的值对应 SkillEnum 表的 `id`：

```csharp
var weaponTable = DataManager.Instance.GetTable<WeaponTable>();
var skillEnumTable = DataManager.Instance.GetTable<SkillEnumTable>();

var weapon = weaponTable.Get(1001);
var rarityInfo = skillEnumTable.Get(weapon.rare);
Debug.Log($"稀有度: {rarityInfo.name}");  // 输出: Common
```

---

## 六、策划日常操作（修改数据后）

### 6.1 方式一：自动导出（推荐）

1. 打开设置面板：编辑器窗口工具栏点 **⚙** 图标
2. 勾选 **监听 Excel 文件变更**
3. 防抖延迟保持默认 500ms
4. 点击 **保存**

之后在 Excel 里修改数据 → 按 Ctrl+S 保存 → 等半秒 → Unity 自动重新导出。**不用再点任何按钮。**

### 6.2 方式二：手动导出

1. 在 Excel 里改完数据，保存
2. 切回 Unity，点工具栏的 **全部导出**

---

## 七、模式 B：程序先写好 C# 类（给程序看的）

如果程序需要自己控制数据结构（用标签标注）：

### 7.1 写 C# 类

```csharp
using ExcelToJsonPlugin.Runtime;

[ExcelTable("Weapon")]
public class WeaponRow
{
    [ExcelColumn("id")]
    public int Id;

    [ExcelColumn("name")]
    public string Name;

    [ExcelColumn("attack")]
    public int Attack;

    [ExcelIgnore]
    public int ComputedValue; // 这列不在 Excel 里
}
```

### 7.2 生成 Excel 模板

1. 点击工具栏的 **模板** 按钮
2. 插件自动在 `Assets/Excel/Templates/` 生成空的 Excel 文件
3. 策划在这个模板里填数据
4. 把填好的 Excel 放到 `Assets/Excel/`

### 7.3 选择映射模式

1. 在文件树中选中对应的 Sheet
2. 上方会显示映射模式下拉框
3. 选择 **模式 B**
4. 点击导出

---

## 八、自定义校验规则（#Rules 表）

在 Excel 里新建一个 Sheet，命名为 `#Rules`，按以下格式写：

| field | rule | params | condition | message |
|-------|------|--------|-----------|---------|
| attack | range | 0~999 | | 攻击力超出范围 |
| price | multiple | 10 | | 价格必须是 10 的倍数 |
| heal | not_empty | | type=2 | 治疗类型必须填治疗量 |
| name | regex | ^[A-Z] | | 名字必须以大写字母开头 |

- `field`：要校验的字段名
- `rule`：规则名（range/regex/multiple/not_empty/required/enum）
- `params`：参数（如 0~999 表示范围 0 到 999）
- `condition`：可选条件（如 type=2 表示只有 type 为 2 的行才执行此规则）
- `message`：自定义错误提示

导出时 `#Rules` 表不会被导出为数据，而是被读取为校验规则。

---

## 九、常见问题

| 问题 | 解决方法 |
|------|---------|
| 菜单里找不到窗口 | 检查 Console 有没有红色错误，截图发我 |
| 导出时报错 | 打开校验 Tab 看具体错误信息，改完 Excel 再导出 |
| 修改 Excel 后数据没更新 | 切回 Unity 等一会儿，或者手动点全部导出 |
| 新加了一个 Sheet 没出现 | 点工具栏的刷新按钮 |
| 数据预览很慢 | 大表自动分页 100 行/页，用搜索功能快速定位 |
| SharpZipLib 报错 | 检查 Dependencies 目录是否有 ICSharpCode.SharpZipLib.dll |

---

## 十、快捷键

| 快捷键 | 功能 |
|--------|------|
| **Ctrl+E** | 导出当前选中的文件 |
| **Ctrl+Shift+E** | 导出全部文件 |
| **Ctrl+V** | 校验全部文件 |
| **Ctrl+R** | 刷新文件列表 |
| **F5** | 刷新文件列表 |

---

> 📧 遇到问题？截图 Console 的错误信息发给我。
